<h1>Burger Land!</h1>

<h2>Burgers to be Devoured!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-block devour=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Burgers that are Devoured!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      {{> burgers/burger-block devour=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="nb">Burger Name:</label>
    <input type="text" id="nb" name="name">
  </div>

  <div class="form-group">
    <label for="devour">Burger Status:</label><br>
    <input type="radio" name="devour" value="0" checked> To Be Devoured!<br>
    <input type="radio" name="devour" value="1" > Devoured!
    
  </div>

  <button type="submit">Add Burger</button>
</form>
